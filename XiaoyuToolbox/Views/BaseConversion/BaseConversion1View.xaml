<UserControl
    x:Class="XiaoyuToolbox.Views.BaseConversion.BaseConversion1View"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:XiaoyuToolbox.Views.BaseConversion"
    d:DesignHeight="600"
    d:DesignWidth="800"
    Background="White"
    FontSize="16"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <vm:BaseConverter1ViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <DataTemplate x:Key="BaseRowTemplate">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>

                <TextBlock VerticalAlignment="Center" Text="{Binding BaseName}" />

                <TextBox
                    Grid.Column="1"
                    Margin="10,0"
                    Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <Button
                    Grid.Column="2"
                    Command="{Binding DataContext.ConvertCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding}"
                    Content="转换" />
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <StackPanel Margin="100,20">
        <ItemsControl ItemTemplate="{StaticResource BaseRowTemplate}" ItemsSource="{Binding BaseItems}" />
    </StackPanel>
</UserControl>
