<UserControl
    x:Class="XiaoyuToolbox.Views.DeviceCheck.MouseCheckView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:XiaoyuToolbox.Views.DeviceCheck"
    d:DesignHeight="600"
    d:DesignWidth="800"
    Background="{StaticResource ViewBackground}"
    FontSize="{StaticResource ViewFontSize}"
    PreviewMouseDown="UserControlPreviewMouseDownOrUp"
    PreviewMouseUp="UserControlPreviewMouseDownOrUp"
    PreviewMouseWheel="UserControlPreviewMouseWheel"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <vm:MouseCheckViewModel />
    </UserControl.DataContext>

    <StackPanel Margin="{StaticResource Margin1}">
        <TextBlock
            Margin="5"
            HorizontalAlignment="Center"
            Text="鼠标检测可以检查鼠标的5个按钮：左键、右键、中键和上下滚轮。" />

        <Grid
            Width="400"
            Height="300"
            Margin="20">
            <Grid.Resources>
                <Style TargetType="Border">
                    <Setter Property="BorderBrush" Value="Black" />
                    <Setter Property="BorderThickness" Value="2" />
                    <Setter Property="Margin" Value="5" />
                </Style>

                <Style TargetType="TextBlock">
                    <Setter Property="VerticalAlignment" Value="Center" />
                    <Setter Property="HorizontalAlignment" Value="Center" />
                </Style>
            </Grid.Resources>

            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="0.5*" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Border Grid.RowSpan="5" Background="{Binding LeftColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <TextBlock Text="左键" />
            </Border>

            <Border
                Grid.RowSpan="5"
                Grid.Column="2"
                Background="{Binding RightColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <TextBlock Text="右键" />
            </Border>

            <Border
                Grid.Row="1"
                Grid.Column="1"
                Background="{Binding WheelUpColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <TextBlock Text="上滚" />
            </Border>

            <Border
                Grid.Row="2"
                Grid.Column="1"
                Background="{Binding MiddleColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <TextBlock Text="中键" />
            </Border>

            <Border
                Grid.Row="3"
                Grid.Column="1"
                Background="{Binding WheelDownColor, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <TextBlock Text="下滚" />
            </Border>
        </Grid>
    </StackPanel>
</UserControl>
